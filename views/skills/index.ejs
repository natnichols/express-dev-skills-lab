<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Dev Skills</title>
</head>
<body>
  <h1>Dev Skills as of <%= time %></h1>
  <ul>
    <% skills.forEach(function(skill) { %>
      <li>
        <form action="/skills/<%= skill._id %>?_method=DELETE" class="delete-form" method="POST">
          <button type="submit">❌</button>
        </form>
        <a href="/skills/<%= skill._id %>/edit"><button class="edit-button">✏️</button></a>
        <a href="/skills/<%= skill._id %>"><%= skill.text %></a>
        - 
        <%= skill.learned ? 'learned in class' : 'not yet learned in class' %>
      </li>
    <% }) %>
  </ul>
  <a href="/skills/new">💡 Add New Skill</a>
</body>
</html>